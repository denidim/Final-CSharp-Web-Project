@using FindATrade.Web.ViewModels.CompanyService;
@model CreateCompanyServiceInputModel

@{
    this.ViewData["Title"] = "Create Service";
}

<h1>@this.ViewData["Title"]</h1>

<hr />

<form method="post" class="col-md-6 ofsset-md-3" enctype="multipart/form-data">
    <div asp-validation-summary="All" class="text-danger"></div>
    <div class="form-group">
        <label asp-for="Title"></label>
        <input asp-for="Title" class="form-control" />
        <span asp-validation-for="Title" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label asp-for="Description"></label>
        <textarea asp-for="Description" class="form-control" rows="3"></textarea>
        <span asp-validation-for="Description" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label asp-for="CategoryId" class="form-label">Genre</label>
        <select asp-for="CategoryId" class="form-control">
            @foreach (var category in Model.Categories)
            {
                <option value="@category.Id">@category.Name</option>
            }
        </select>
        <span asp-validation-for="CategoryId" class="text-danger"></span>
    </div>
    <div class="form-group">
        <label asp-for="Images"></label>
        <input asp-for="Images" class="form-control" />
        <span asp-validation-for="Images" class="text-danger"></span>
    </div>
    <hr />
    <h6>you can add up to 3 packages otherwise the default package (price rate) will display as negotiable</h6>
    @for (int i = 0; i < 3; i++)
    {
        <div class="form-group">
            <label asp-for="Packages[i].Description"></label>
            <input asp-for="Packages[i].Description" class="form-control" />
            <span asp-validation-for="Packages[i].Description" class="text-danger"></span>
        </div>
        <div class="form-group">
            <label asp-for="Packages[i].Price"></label>
            <input asp-for="Packages[i].Price" class="form-control" />
            <span asp-validation-for="Packages[i].Price" class="text-danger"></span>
        </div>
        <hr />
    }
    
    <input type="submit" class="button btn-primary" value="Add" />
</form>

@*<div id="PackagesContainer">
    <label asp-for="Packages"></label>
    <a class="btn btn-success" onclick="Add()">+</a>
    <a class="btn btn-danger" onclick="Remove()">-</a>
    <span asp-validation-for="Packages" class="text-danger"></span>
</div>
@section Scripts{
    <script>
        //Packages[0].Price
        //Packages[0].Description
        var index = 0;
        function Add() {
            $("#PackagesContainer")
                .append("<input class='form-group' type='text' name='Packages[" + index + "].Price' /> <input class='form-group' type='text' name='Packages[" + index + "].Description' /></div>");
            index++;
        }

        function Remove() {
            for (let i = 0; i < 2; i++) {
                $("#PackagesContainer > input:last-child")
                .remove();
            index--;
            }
        }
    </script>
}*@